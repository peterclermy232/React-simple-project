import React, { useContext } from 'react'

function FavoritesPage() {
    const favoritesCtx = useContext(F)
    return (
        <div>
            Favorites Page
        </div>
    )
}

export default FavoritesPage
